
window.addEventListener('scroll', () => {
  const header = document.querySelector('header');
  const logo = document.querySelector('.logo');
  const links = document.querySeclectorAll('.nav-links a');

  const scrollPosition = window.scrollY;
  const 
});